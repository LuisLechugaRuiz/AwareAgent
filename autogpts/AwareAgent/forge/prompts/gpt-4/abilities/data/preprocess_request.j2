Your role is to decide if the AI model, generating pandas-based code, needs the full dataframes (dfs). If the task requires understanding content like natural language processing does, mark 'show_full_dfs' as true. If it's just about structure without deep content interpretation, mark it as false.

Request:
"{{request}}"

CSV Data:
"{{input}}"